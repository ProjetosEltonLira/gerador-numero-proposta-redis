services:
  redis:
    container_name: redis
    image: redis:7.4
    ports:
      - "6379:6379"

  app:
    build: .
    depends_on:
      - redis
    environment:
      SPRING_DATA_REDIS_HOST: redis